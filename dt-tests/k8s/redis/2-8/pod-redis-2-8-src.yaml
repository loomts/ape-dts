apiVersion: v1
kind: Pod
metadata:
  name: pod-redis-2-8-src
  namespace: dts 
  labels:
    app: redis
    version: "2-8"
    use: src
spec:
  containers:
  - name: pod-redis-2-8-src
    image: redis:2.8.22
    ports:
    - containerPort: 6379
      protocol: TCP 
    command: ["redis-server"]